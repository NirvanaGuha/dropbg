{
  "name": "dropbg",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "deploy:cf": "SITE_URL=${SITE_URL:-https://dropbg.app/} vite build && npx wrangler pages deploy dist --project-name dropbg --branch main --commit-dirty=true",
    "deploy:gh": "SITE_URL=${SITE_URL:-https://dropbg.app/} vite build && node scripts/deploy.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": "true",
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@imgly/background-removal": "^1.7.0",
    "fflate": "^0.8.3"
  }
}
